// 自定义的配置项gradle文件，需要在【根gradle】中的buildscript中使用apply from 引入。
ext {
    compileSdkVersion = 28
    buildToolsVersion = "28.0.3"
    supprotVersion = "28.0.0"
    minSdkVersion = 15
    targetSdkVersion = 24

    gradle_plugin = "4.0.0"
    maven_plugin = "2.0"
    greendao_plugin = "3.3.0"

    versionCode = 1
    versionName = "v1.0"
    release_path = '../.release_apk'
}

